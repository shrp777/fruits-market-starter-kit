meta {
  name: Read Products
  type: http
  seq: 1
}

get {
  url: {{host}}/products
  body: none
  auth: inherit
}

assert {
  res.body.success: eq true
  res.body.data: isArray
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
