meta {
  name: Health Check
  type: http
  seq: 2
}

get {
  url: {{host}}/health
  body: none
  auth: inherit
}

assert {
  res.body.success: eq true
  res.body.status: eq healthy
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
